# Core dependencies
# NOTE: Pydantic version is set to 1.10.12 (v1) to be compatible with redis-om==0.2.1
# If you want to use Pydantic v2, you'll need to replace redis-om with a compatible alternative
pydantic==1.10.12
notion-client==2.0.0
python-dotenv==1.0.0
# aiohttp==3.9.1 # Potentially unused or transitive
loguru==0.7.2
# requests==2.31.0 # Potentially unused or transitive
# NOTE: FastAPI version adjusted to be compatible with Pydantic v1
fastapi==0.95.2
uvicorn==0.23.2
gunicorn==21.2.0
python-dateutil==2.8.2
# jwt==1.3.1 # Potentially unused or transitive
# PyJWT==2.7.0 # Potentially unused or transitive; python-jose is kept as more likely for FastAPI

# Database and validation
sqlalchemy==2.0.23
alembic==1.12.1
email-validator==2.0.0
# NOTE: Using pydantic-settings v1 since we're using Pydantic v1
pydantic-settings==1.0.0
pymongo==4.6.1
motor==3.3.2
redis==5.0.1
redis-om==0.2.1  # Redis Object Mapper for Python

# AI Providers
openai==0.27.8
# anthropic==0.2.8 # Potentially unused or transitive

# Hugging Face Pro
huggingface-hub>=0.19.4
transformers>=4.35.2
datasets>=2.15.0
gradio>=4.11.0
smolagents>=0.0.3

# Vector database and embeddings
pgvector==0.2.4
sentence-transformers==2.2.2
numpy>=1.25.0
nltk==3.8.1
colorama==0.4.6

# RAG dependencies
# crawl4ai==0.5.2  # Temporarily commented out due to compatibility issues
# aqua-client==0.3.1  # Temporarily commented out due to compatibility issues

# Integration services
pyairtable==1.4.0
woocommerce==3.0.0
# aiofiles==23.1.0 # Potentially unused or transitive
beautifulsoup4==4.12.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.24.1

# Security
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# Docker deployment
python-multipart==0.0.6

# MoneyPrinterTurbo integration
httpx==0.24.1
moviepy==2.1.2
edge-tts==6.1.19
