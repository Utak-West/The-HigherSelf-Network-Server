name: Docker Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/higherself-network-server:latest
            ghcr.io/${{ github.repository_owner }}/higherself-network-server:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update Notion Deployment Status
        if: github.event_name != 'pull_request'
        run: |
          echo "Updating Notion about deployment status..."
          echo "IMPORTANT: Notion serves as the central hub for all data and workflows"
          
          # This would be replaced with actual Notion API call in production
          # Example using curl and environment variables
          if [ -n "${{ secrets.NOTION_API_TOKEN }}" ] && [ -n "${{ secrets.NOTION_DEPLOYMENT_DATABASE_ID }}" ]; then
            curl -X POST https://api.notion.com/v1/pages \
              -H "Authorization: Bearer ${{ secrets.NOTION_API_TOKEN }}" \
              -H "Content-Type: application/json" \
              -H "Notion-Version: 2022-06-28" \
              --data '{
                "parent": { "database_id": "${{ secrets.NOTION_DEPLOYMENT_DATABASE_ID }}" },
                "properties": {
                  "Name": {
                    "title": [
                      {
                        "text": {
                          "content": "Deployment ${{ github.sha }}"
                        }
                      }
                    ]
                  },
                  "Status": {
                    "select": {
                      "name": "Completed"
                    }
                  },
                  "Timestamp": {
                    "date": {
                      "start": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
                    }
                  }
                }
              }'
          else
            echo "Notion credentials not configured, skipping update"
          fi

  security-scan:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run security scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'ghcr.io/${{ github.repository_owner }}/higherself-network-server:${{ github.sha }}'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'